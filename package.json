{
  "name": "project1",
  "version": "1.0.0",
  "description": "Check-in 1: Random Fox Gallery",
  "type": "module",
  "scripts": {
    "start": "npx web-dev-server --open --node-resolve",
    "build": "echo 'No build step needed'"
  },
  "dependencies": {
    "lit": "^3.1.0"
  },
  "devDependencies": {
    "@web/dev-server": "^0.4.2"
  }
}
